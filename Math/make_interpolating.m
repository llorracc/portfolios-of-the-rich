(*% Construct the data and interpolating functions for each of the important items. % Note that the function f(.) to be interpolated must be the last element in the list,% so a seaparate list is generated for each of the variables we want to approximate. *)Print["Running make_interpolating.m"];If[StageOfLife > LastElementLessThan[StartOfLifeStage,LifePosn],  (* then we have moved into a new stage of life *)    StageOfLife--;    epDrawGrid = epVals[[LifePosn,1,1]];    MakeArgArrays[ActualFunctions];       (* When we change the set of possible values for epDraw, need to redo matrices *)];If[SolutionMethod == Value,<<make_interpolating_Value.m];If[SolutionMethod == FOC,  <<make_interpolating_FOC.m];If[SolutionMethod == Both, <<make_interpolating_Value.m;<<make_interpolating_FOC.m];If[VerboseOutput == True,Print["Done with make_interpolating.m"]];